---
import products from "../data/products.json";
import { Image } from "astro:assets";
---

<section id='produk' class='py-16 md:scroll-mt-2'>
  <div class='container mx-auto px-6'>
    <div class='text-center mb-12'>
      <h2 class='text-3xl md:text-4xl font-bold text-dark mb-4'>Produk Kami</h2>
      <div class='w-20 h-1 bg-primary mx-auto'></div>
      <p class='text-gray-600 mt-4 max-w-2xl mx-auto'>
        Kami menyediakan berbagai jenis tinta bekas printer dengan kualitas terbaik dari merek-merek ternama
      </p>
    </div>
    <div class='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8'>
      {
        products.map((product) => (
          <div class='bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition duration-300'>
            <Image src={product.imageUrl} alt='Tinta HP' width={100} height={100} class='w-full h-48 object-cover' priority />
            <div class='p-6'>
              <h3 class='text-xl font-bold text-dark mb-2'>{product.name}</h3>
              <p class='text-gray-600 mb-4'>Tinta bekas original Brother 5000 dengan sisa 80-90% kapasitas</p>
              <div class='flex justify-between items-center'>
                <span class='text-primary font-bold text-lg'>{product.price}</span>
                <button class='bg-primary hover:bg-primary-dark text-white font-bold py-2 px-4 rounded transition duration-300'>Beli Sekarang</button>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
